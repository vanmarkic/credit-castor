const admin = require('firebase-admin');

// Initialize Firebase Admin (you'll need to set up credentials)
// For now, let's just check if we can access the document structure
console.log('To check Firestore document, use Firebase Console or:');
console.log('1. Go to https://console.firebase.google.com/project/credit-castor/firestore');
console.log('2. Navigate to projects/shared-project');
console.log('3. Check if projectParams.maxTotalLots exists and its value');
